{
  "name": "session-webhook-server",
  "version": "1.0.0",
  "description": "Webhook server for Session.js - Expose webhooks for Session functions and forward notifications",
  "main": "session-webhook-server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run session-webhook-server.ts",
    "dev": "bun --watch session-webhook-server.ts",
    "test": "bun test",
    "typecheck": "bun tsc --noEmit"
  },
  "keywords": [
    "session",
    "webhook",
    "messaging",
    "bot"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@session.js/client": "latest",
    "@session.js/file-keyval-storage": "latest",
    "express": "^4.18.3",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "typescript": "^5.4.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
